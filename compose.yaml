services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=$DB_SCHEMA'
      - 'MYSQL_ROOT_PASSWORD=$DB_PASSWORD'
      - 'MYSQL_USER=$DB_USERNAME'
    ports:
      - '3306:3306'
